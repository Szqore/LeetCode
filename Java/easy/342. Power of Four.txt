class Solution { //task 342
    public boolean isPowerOfFour(int n) {
        // n > 0 и проверка, что это степень двойки, и проверка позиции бита
        return n > 0 && (n & (n - 1)) == 0 && (n & 0x55555555) != 0;
    }
}
